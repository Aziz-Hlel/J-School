const DeleteScheduleExam = ({
  classroomId,
  examId,
  setIsDeleteOpen,
}: {
  classroomId: string;
  examId: string;
  setIsDeleteOpen: (open: boolean) => void;
}) => {
  return <div>DeleteScheduleExam</div>;
};

export default DeleteScheduleExam;
